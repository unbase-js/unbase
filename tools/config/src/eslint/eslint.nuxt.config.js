import { createConfigForNuxt } from '@nuxt/eslint-config'

/**
 * A custom ESLint configuration for libraries that use Nuxt.js.
 *
 * @type {import("eslint").Linter.Config[]}
 * */
export const nuxtESLintConfig = createConfigForNuxt()
